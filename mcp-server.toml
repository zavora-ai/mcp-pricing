server_id = "mcp_pricing"
display_name = "Pricing Engine"
version = "2.1.0"
domain = "pricing"
sdk_version = "0.1.0"
risk_level = "medium"
writes_allowed = "gated"
transports = ["stdio"]
credentials = []
governance_gates = ["audit_all_changes"]
environments = ["development", "staging", "production"]

[[tools]]
name = "price_calculate"
description = "Calculate price through full waterfall with CEL rules"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "rules_validate"
description = "Validate a CEL expression"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "catalog_upsert"
description = "Add/update product in catalog"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "catalog_get"
description = "Get product by SKU"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "catalog_list"
description = "List all products"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "rules_create"
description = "Create pricing rule with CEL condition"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "rules_list"
description = "List all pricing rules"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "rules_activate"
description = "Activate a pricing rule"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "rules_deactivate"
description = "Deactivate a pricing rule"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "segments_create"
description = "Create customer segment"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "segments_list"
description = "List customer segments"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "promotions_create"
description = "Create promotion"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "promotions_list"
description = "List promotions"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "promotions_apply"
description = "Apply promotion code to SKU"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "quotes_create"
description = "Create a price quote"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "quotes_get"
description = "Get quote by ID"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "quotes_approve"
description = "Approve a quote"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "market_fx_convert"
description = "Convert currency (170+ currencies)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "market_fx_rates"
description = "Get live FX rates"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "market_tax"
description = "Calculate tax by country"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "audit_log"
description = "Query audit trail"
risk_class = "read_only"
requires_approval = false
credential_bindings = []
